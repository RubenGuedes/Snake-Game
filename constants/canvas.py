from constants.percent import PR_88, PR_70
from constants.window_dim import WIDTH_, HEIGHT 

# Salto e Tamanho do lado para desenhar o Mapa
JUMP = 15
WIDTH_CANVAS_ = HEIGHT_CANVAS = HEIGHT * PR_88
MAXSIDE_CANVAS = int(WIDTH_ * PR_70) + 1